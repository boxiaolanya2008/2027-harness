
# AI-Agent 助手编码规范

**生效时间：** 2026-09-19 23:54:20 CST
**强制等级：** 最高优先级，所有 Agent 运行全流程必须严格遵守

---

## 1. 注释强制规范

| 要求项 | 执行细则 | 违规判定标准 |
| --- | --- | --- |
| 禁止滥用特殊符号作为注释 | 不得将 `//` `/* */` `#` `--` `<!-- -->` 以外的任何特殊符号（包括但不限于 `*` `-` `~` `#` 单独成行、无意义 emoji、自定义标记符）当作注释使用 | 任何非标准注释语法的符号都不允许作为注释标识 |
| 注释内容合规约束 | 所有注释必须完全对齐 AI 知识库中对应编程语言的官方注释规范，注释仅用于标注代码逻辑说明、参数含义、遗留待办、版本变更四类场景 | 禁止无意义占位注释、冗余重复注释、与代码逻辑无关的注释内容 |
| 注释排版要求 | 单行注释与代码行末尾保持至少 2 个空格间隔，块级注释上下保留空行，注释缩进与对应代码块完全对齐 | 注释排版凌乱、与代码缩进错位属于违规 |

### 1.1 注释规范补充说明

注释是代码与开发者之间最直接的沟通桥梁，其价值在于**降低理解成本**，而非填充版面。因此，所有 Agent 在生成或修改代码时，必须将注释视为代码逻辑的一部分，而非可有可无的附属品。

- **注释语言一致性：** 同一文件内注释语言必须统一，禁止中英文混杂导致语义割裂。
- **注释时效性：** 代码逻辑变更时，必须同步更新对应注释，禁止出现注释与代码逻辑相悖的情况。
- **注释密度控制：** 注释应覆盖关键逻辑节点，但不得逐行注释显而易见的代码，避免造成视觉噪音。
- **TODO 注释规范：** 遗留待办注释必须包含负责人、预计完成时间与具体事项，格式建议为 `// TODO(负责人, YYYY-MM-DD): 具体事项`。

---

## 2. 代码美观度与仓库文档规范

### 2.1 代码风格强制标准

所有代码无论复杂度、行数多少，必须优先保证美观大方：

- 统一使用 **4 空格缩进**，禁止混合使用 Tab 与空格。
- 每个代码逻辑块（函数、条件分支、循环）前后保留 **1 行空行**分隔。
- 变量、函数命名遵循对应语言的业界通用大驼峰/小驼峰/下划线规范，禁止无意义短命名。
- 每行代码长度不超过 **120 字符**，超长参数自动换行对齐，禁止单行堆叠大量逻辑。
- 代码行数无上限限制，优先用拆分清晰的易读逻辑替代晦涩精简的压缩写法，**绝不以减少代码行为代价牺牲可读性与美观度**。

#### 2.1.1 代码风格执行细则

- **导入顺序：** 标准库、第三方库、本地模块依次分组，组间空行分隔。
- **函数长度：** 单个函数建议不超过 80 行，超出时应拆分为多个职责单一的子函数。
- **嵌套深度：** 条件与循环嵌套建议不超过 4 层，超出时应通过提前返回或提取函数降低嵌套。
- **魔法数字：** 禁止在代码中直接出现无说明的魔法数字，必须提取为具名常量。
- **异常处理：** 禁止空捕获异常，捕获后必须记录日志或进行有效处理。

### 2.2 AGENT.md 代码规范一致性保障文档规则

自动在仓库根目录创建 `AGENT.md` 文件，作为**全仓库代码规范一致性保障与踩坑问题归档**的核心文档。其核心目标是：**确保所有 Agent 在不同时间、不同模块、不同迭代中产出的代码，始终保持统一风格与统一规范，避免出现规范漂移与代码风格分裂。**

#### 2.2.1 规范一致性保障

- **统一风格基准：** `AGENT.md` 中必须明确记录本仓库采用的代码风格基准（缩进、命名、注释、导入顺序等），所有 Agent 在生成代码前必须先读取并严格对齐该基准。
- **跨模块一致性：** 不同模块、不同 Agent 产出的代码，在命名习惯、注释格式、目录结构上必须保持统一，禁止同一仓库内出现多种风格并存的混乱局面。
- **规范漂移拦截：** 当检测到新提交代码与 `AGENT.md` 中记录的规范基准不一致时，Agent 应自动拦截并提示修正，避免规范在迭代中被逐步稀释。
- **规范版本记录：** 每次规范调整必须在 `AGENT.md` 中记录变更时间、变更内容与变更原因，确保规范演进过程可追溯。

#### 2.2.2 踩坑问题归档

- 仅写入用户真实运行使用后复现、经过问题核对确认的线上故障、兼容性问题、逻辑 BUG。
- 每条坑点必须包含：**问题复现场景、根因定位、修复方案、规避前置校验规则** 4 个必填字段。
- 禁止写入未经实际验证的臆想问题、未复现的偶发异常猜测，所有入库坑点必须绑定对应的用户操作记录与截图凭证。
- 每次修复线上问题后 **24 小时内**必须同步更新该文档，确保后续 Agent 开发时直接规避同类问题。

#### 2.2.3 AGENT.md 文档结构建议

```markdown
# AGENT.md - 代码规范一致性保障与踩坑归档

## 一、代码规范基准

### 1.1 风格基准
- 缩进：
- 命名：
- 注释：
- 导入顺序：

### 1.2 规范变更记录

| 变更时间 | 变更内容 | 变更原因 |
| --- | --- | --- |
|  |  |  |

## 二、踩坑问题归档

### 坑点索引

| 编号 | 问题简述 | 影响范围 | 状态 |
| --- | --- | --- | --- |
| BUG-001 | 示例问题 | 模块A | 已修复 |

### BUG-001：问题简述

- **问题复现场景：**
- **根因定位：**
- **修复方案：**
- **规避前置校验规则：**
- **关联凭证：**
```

---

3. 功能文档强制关联规则

所有新增文件、新增功能迭代必须严格执行文档同步机制：

· 仓库根目录下固定创建 docs/ 专属文档文件夹，与 src 源代码目录完全同级。
· 每新增 1 个代码文件、每上线 1 个独立功能模块，必须在 docs/ 下创建对应命名的 .md 说明文档，命名规则为 [功能模块名]-功能说明.md。
· 每份功能文档必须包含：模块核心作用、依赖的上下游模块、核心函数入参出参定义、逻辑跳转流程图、修改注意事项 5 个核心部分。
· 功能代码完成提交前，必须先完成对应文档的编写提交，无配套文档的代码提交视为不规范，Agent 自动拦截不执行合并操作。
· 后续修改原有功能逻辑时，必须同步更新对应的功能文档，确保文档与实际代码逻辑 100% 对齐，避免出现文档和代码脱节的情况。

3.1 功能文档模板

```markdown
# [功能模块名]-功能说明

## 一、模块核心作用

## 二、依赖的上下游模块

## 三、核心函数入参出参定义

## 四、逻辑跳转流程图

## 五、修改注意事项
```

3.2 文档同步执行细则

· 文档版本管理： 功能文档需随代码一同纳入版本控制，禁止脱离仓库单独存放。
· 文档评审机制： 文档提交前需经过至少一次自检，确保五大核心部分无遗漏。
· 文档更新时效： 功能逻辑变更后，文档更新必须在同一提交或同一迭代周期内完成。
· 文档可读性： 流程图建议使用 Mermaid 语法绘制，确保在主流 Markdown 渲染器中可直接展示。

---

4. 规范执行与监督机制

· 自动拦截： Agent 在代码提交、合并等关键节点自动校验上述规范，不合规操作直接拦截。
· 违规记录： 所有违规行为需记录至仓库 AGENT.md 或独立审计日志，便于后续追溯与优化。
· 持续迭代： 本规范将根据实际运行反馈持续修订，修订后需同步更新所有相关文档与 Agent 校验逻辑。
· 责任归属： 每个 Agent 实例对其生成代码的规范合规性负全责，不得以“自动生成”为由规避规范约束。

---

本规范自生效时间起强制执行，最终解释权归仓库维护方所有。
