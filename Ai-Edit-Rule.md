AI-Agent 助手编码规范

生效时间：2026-09-19 23:54:20 CST | 强制等级：最高优先级，所有Agent运行全流程必须严格遵守

 

1. 注释强制规范

要求项执行细则违规判定标准 禁止滥用特殊符号作为注释不得将// /* */ # -- <!-- --> 以外的任何特殊符号（包括但不限于* - ~ #单独成行、无意义emoji、自定义标记符）当作注释使用任何非标准注释语法的符号都不允许作为注释标识 注释内容合规约束所有注释必须完全对齐AI知识库中对应编程语言的官方注释规范，注释仅用于标注代码逻辑说明、参数含义、遗留待办、版本变更四类场景禁止无意义占位注释、冗余重复注释、与代码逻辑无关的注释内容 注释排版要求单行注释与代码行末尾保持至少2个空格间隔，块级注释上下保留空行，注释缩进与对应代码块完全对齐注释排版凌乱、与代码缩进错位属于违规 

 

2. 代码美观度与仓库文档规范

2.1 代码风格强制标准
所有代码无论复杂度、行数多少，必须优先保证美观大方：

* 统一使用4空格缩进，禁止混合使用Tab与空格
* 每个代码逻辑块（函数、条件分支、循环）前后保留1行空行分隔
* 变量、函数命名遵循对应语言的业界通用大驼峰/小驼峰/下划线规范，禁止无意义短命名
* 每行代码长度不超过120字符，超长参数自动换行对齐，禁止单行堆叠大量逻辑
* 代码行数无上限限制，优先用拆分清晰的易读逻辑替代晦涩精简的压缩写法，绝不以减少代码行为代价牺牲可读性与美观度

2.2 AGENT.md 仓库踩坑文档规则
自动在仓库根目录创建AGENT.md文件，作为全局踩坑问题归档文档：

仅写入用户真实运行使用后复现、经过问题核对确认的线上故障、兼容性问题、逻辑BUG
每条坑点必须包含：问题复现场景、根因定位、修复方案、规避前置校验规则4个必填字段
禁止写入未经实际验证的臆想问题、未复现的偶发异常猜测，所有入库坑点必须绑定对应的用户操作记录与截图凭证
每次修复线上问题后24小时内必须同步更新该文档，确保后续Agent开发时直接规避同类问题

 

3. 功能文档强制关联规则
所有新增文件、新增功能迭代必须严格执行文档同步机制：

仓库根目录下固定创建docs/专属文档文件夹，与src源代码目录完全同级
每新增1个代码文件、每上线1个独立功能模块，必须在docs/下创建对应命名的.md说明文档，命名规则为[功能模块名]-功能说明.md
每份功能文档必须包含：模块核心作用、依赖的上下游模块、核心函数入参出参定义、逻辑跳转流程图、修改注意事项5个核心部分
功能代码完成提交前，必须先完成对应文档的编写提交，无配套文档的代码提交视为不规范，Agent自动拦截不执行合并操作
后续修改原有功能逻辑时，必须同步更新对应的功能文档，确保文档与实际代码逻辑100%对齐，避免出现文档和代码脱节的情况